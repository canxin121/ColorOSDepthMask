name: Build release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag, for example v1.0.0
        required: true
        default: v1.0.0
      source_ref:
        description: Private source branch, tag or commit
        required: true
        default: main
      prerelease:
        description: Mark as prerelease
        required: true
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout release repository
        uses: actions/checkout@v4

      - name: Checkout private source
        uses: actions/checkout@v4
        with:
          repository: canxin121/ColorOSDepthMask-Source
          ref: ${{ inputs.source_ref }}
          ssh-key: ${{ secrets.SOURCE_DEPLOY_KEY }}
          path: source

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
          cache-dependency-path: source/**/*.gradle*

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 37
        shell: bash
        run: |
          set -euo pipefail
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager \
            "platform-tools" \
            "platforms;android-37" \
            "build-tools;37.0.0"

      - name: Build release APK
        working-directory: source
        run: |
          set -euo pipefail
          chmod +x gradlew
          test -f signing/release.jks
          test -f signing/release.properties
          ./gradlew :app:assembleRelease --no-daemon --stacktrace

      - name: Validate artifact
        id: artifact
        shell: bash
        run: |
          set -euo pipefail
          APK='source/app/build/outputs/apk/release/app-release.apk'
          test -f "$APK"
          test "$(unzip -p "$APK" assets/xposed_init | tr -d '\r\n')" = 'dev.coloros.depthmask.hook.DepthMaskHook'
          NAME="ColorOSDepthMask-${{ inputs.tag }}.apk"
          cp "$APK" "$NAME"
          sha256sum "$NAME"
          echo "apk=$NAME" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          name: ColorOSDepthMask ${{ inputs.tag }}
          prerelease: ${{ inputs.prerelease }}
          generate_release_notes: true
          files: ${{ steps.artifact.outputs.apk }}
